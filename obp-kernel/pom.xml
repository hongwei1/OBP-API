<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.tesobe</groupId>
        <artifactId>obp-parent</artifactId>
        <relativePath>../pom.xml</relativePath>
        <version>1.10.1</version>
    </parent>
    <artifactId>obp-kernel</artifactId>
    <packaging>jar</packaging>
    <name>Open Bank Project Kernel</name>

    <!--
      The API vocabulary that has no application behind it: error messages, tags, formats.

      The point of this module is the dependency arrow, not the code. Everything in obp-api can
      see obp-kernel; nothing in obp-kernel can see obp-api, and the compiler is what enforces
      that rather than a reviewer noticing. In a single module, "ErrorMessages must not reach
      into the connector" is a convention; here it is a build failure.

      What may live here: values and pure functions with no dependency on persistence, HTTP, the
      connector, or any API standard. What may not: anything that needs a CallContext, a
      database, or a version package. If something "almost" fits, it does not fit — the value of
      this module is entirely in the things it refuses.

      Deliberately NOT obp-commons: that module already depends on lift-persistence, so it is not
      a place a persistence-free kernel can live.

      Classes here keep the package names they had in obp-api (code.api.util). A package spanning
      two jars on one classpath is fine, and it means none of the 176 files that import
      ErrorMessages had to change. The module boundary is the point; churn across the call sites
      would have been pure cost.
    -->
    <dependencies>
        <dependency>
            <groupId>com.tesobe</groupId>
            <artifactId>obp-commons</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.json4s</groupId>
            <artifactId>json4s-native_${scala.version}</artifactId>
        </dependency>
        <dependency>
            <groupId>org.scalatest</groupId>
            <artifactId>scalatest_${scala.version}</artifactId>
        </dependency>
        <dependency>
            <groupId>org.scalactic</groupId>
            <artifactId>scalactic_${scala.version}</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
                <configuration>
                    <skipTests>true</skipTests>
                </configuration>
            </plugin>
            <plugin>
                <groupId>net.alchim31.maven</groupId>
                <artifactId>scala-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.scalatest</groupId>
                <artifactId>scalatest-maven-plugin</artifactId>
                <configuration>
                    <reportsDirectory>${project.build.directory}/surefire-reports</reportsDirectory>
                    <forkMode>once</forkMode>
                    <junitxml>.</junitxml>
                    <filereports>WDF TestSuite.txt</filereports>
                    <argLine>-Drun.mode=test -XX:MaxMetaspaceSize=512m -Xms512m -Xmx512m</argLine>
                    <tagsToExclude>code.external</tagsToExclude>
                    <testFailureIgnore>${maven.test.failure.ignore}</testFailureIgnore>
                </configuration>
                <executions>
                    <execution>
                        <id>test</id>
                        <goals>
                            <goal>test</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>

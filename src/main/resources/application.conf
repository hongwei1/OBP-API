system-name = "OBP-Scala-South"

akka {
  loglevel = INFO
}

akka.kafka.consumer {
  kafka-clients {
    auto.commit.interval.ms = 10000
  }
}
//This is the URL for the kafka server
bootstrap {
  servers = "localhost:9092"
}

kafka {
  partitions = 10
  version = "Jun2017"
  request {
    topic{
      request = "Request"
      response = "Response"
    }
    target {
      source = "example_import*.json"
    }

    generic {
      name = "localFile"
      timeout = 2
    }
  }
}
//This is the base URL for calls to the mainframe
mockserver{
  run = true
}
bankserver {
  //url = "http://ntas107703t55:7800/ESBLeumiDigitalBank/PAPI"
  url = "http://localhost:1080"
}
